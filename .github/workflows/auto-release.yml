name: 🧠 Automated Weekly Release

on:
  schedule:
    - cron: "0 0 * * 0" # Runs every Sunday midnight (UTC)
  workflow_dispatch: # Allows manual trigger from GitHub Actions UI

permissions:
  contents: write # ✅ Needed for committing & tagging
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🧩 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22 # ✅ Node 22 fixes regex issues

      - name: 🔧 Configure Git identity
        run: |
          git config --global user.name "Zeeshan Khan (CI Bot)"
          git config --global user.email "zkhan@360healthtek.com"
          git config --global --add safe.directory /github/workspace
          echo "✅ Git identity configured:"
          git config --list | grep user

      - name: 📥 Install dependencies
        run: npm ci

      - name: 🚀 Run release-it (automated)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx release-it --ci

      - name: 📨 Notify completion
        if: always()
        run: |
          echo "✅ Automated Release completed!"
          echo "🔗 View CHANGELOG: https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md"
